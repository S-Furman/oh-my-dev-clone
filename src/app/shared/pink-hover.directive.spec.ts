import { PinkHoverDirective } from './pink-hover.directive';

describe('PinkHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PinkHoverDirective();
    expect(directive).toBeTruthy();
  });
});
